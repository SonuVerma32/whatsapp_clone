import 'package:flutter/material.dart';

class CallsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: new Text("Calls Screen",style: new TextStyle(fontSize: 20.0),),
    );
  }
}
